# System Components

| Component | Role | Location |
|---|---|---|
| **Mobile Chat UI tree** | Parallel React Native implementation of desktop's `ChatInterface` component tree. Built with `@rn-primitives/*` + uniwind + Reanimated. | `apps/mobile/components/chat/` (NEW) |
| **Host-service tRPC client** | Typed HTTP+tRPC client against `@superset/host-service`'s `AppRouter`. Desktop uses Electron IPC (`ipcLink`) for chat communication; mobile adapts the same `AppRouter` contract to HTTP routed through `apps/relay` (per-host WS tunnel) with JWT bearer auth — a deliberate mobile-specific transport adaptation, not a mirror of desktop's IPC layer. | `apps/mobile/lib/host-service-client.ts` (NEW) |
| **Electric collections (extended)** | Adds `chat_sessions` collection to existing collections graph for realtime session-list sync. Already-exposed Electric shape; mobile just consumes it. | `apps/mobile/lib/collections/collections.ts` (MODIFY) |
| **Tiptap editor wrapper** | WebView-hosted Tiptap via `@10play/tentap-editor` configured with the same minimal extension set as desktop's `TiptapPromptEditor.tsx` plus the `SlashCommandNode` and `FileMentionNode` ports. | `apps/mobile/components/chat/ChatInputFooter/` (NEW) |
| **Mid-turn pause UI** | Container shape chosen per interaction (see `07-uc-pause.md` Design Rationale for citations). Tool approval = inline card + sticky thumb-docked footer (Continue.dev pattern); ask_user = `@gorhom/bottom-sheet` with `BottomSheetTextInput` (keyboard handling); plan approval = full-screen modal as expo-router pushed route (Apple HIG "in-depth content"). Plus a floating pending-action indicator for off-screen pauses. | `apps/mobile/components/chat/PendingApprovalCard/`, `PendingApprovalFooter/` (sticky), `PendingQuestionSheet/`, `PendingActionIndicator/` (NEW components); `apps/mobile/app/(authenticated)/chat/[sessionId]/plan-review/[planId].tsx` (NEW pushed route) |
| **Message list (virtualized)** | `@shopify/flash-list` (inverted) with Reanimated scroll-back affordance. | `apps/mobile/components/chat/MessageList/` (NEW) |
| **Markdown renderer** | RN markdown rendering (likely `react-native-markdown-display` or `@expensify/react-native-live-markdown`) for assistant message content. Replaces desktop's web-only `streamdown`. | `apps/mobile/components/chat/MessageMarkdown/` (NEW) |
| **Push notification handler (mobile)** | Permission-flow controller (pre-prompt → `requestPermissionsAsync` → grant/deny handling), Expo push token registration against `apps/relay`, route-aware `setNotificationHandler` for foreground suppression, and `addNotificationResponseReceivedListener` that dispatches taps to the UC-NAV-05 deep-link router. Cold-launch status re-check with cleanup paths for OS-revoked permission. | `apps/mobile/lib/push-notifications/` (NEW) |
| **Push pre-prompt screen** | In-app screen shown before the OS notification-permission dialog the first time the user enters a chat session. Explains the value, offers Enable / Not now. Triggered from the session-entry hook when status is `undetermined`. | `apps/mobile/screens/(authenticated)/(chat)/components/PushPrePromptScreen/` (NEW) |
| **Relay push fanout module** | Handles inbound `push:lifecycle` upstream tunnel-WS messages from hosts (new message type), looks up registered Expo push tokens for the matching org+user in Upstash KV, calls Expo Push API (`https://exp.host/--/api/v2/push/send`), and drops tokens that return `DeviceNotRegistered`. | `apps/relay/src/push.ts` (NEW); tunnel-message wiring in `apps/relay/src/tunnel.ts` (MODIFY) |
| **Push token registration endpoints** | New Hono routes on relay using the existing JWT auth middleware (`apps/relay/src/auth.ts` `verifyJWT`): `POST /push/register` (mobile registers an Expo token after permission grant) and `DELETE /push/register/:deviceId` (mobile de-registers on logout / OS-revoked permission). Tokens stored in Upstash KV keyed by `push:org:{orgId}:user:{userId}` as a set of `{ token, platform, deviceId, lastSeenAt }`. | `apps/relay/src/index.ts` (MODIFY — add routes); `apps/relay/src/push.ts` (NEW — handler logic) |
| **Reconnect/cursor protocol** | On foreground-from-background or host-online detection, re-fetches `chat.getSnapshot` and reconciles local optimistic state with cursor/offset semantics. | `apps/mobile/screens/(authenticated)/chat/[sessionId]/hooks/useSessionResume.ts` (NEW) |
| **Sessions list screen** | Default landing for the Chat tab. Renders a **flat, recency-sorted `FlashList`** of sessions scoped to the selected project (UC-NAV-01) — no workspace sectioning, no sticky headers, no per-section pagination. Header composition: a title row with the project chip (UC-NAV-08 surface — static label when org has 1 project, tappable chip + bottom sheet when ≥2); a second row with the search `TextInput` (UC-NAV-07) and a ⚙ filter button (UC-NAV-08 trigger, with `·N` badge when filters are active); and an applied-filter chip tag row below when ≥1 filter is active (UC-NAV-08). FAB (UC-NAV-04 trigger) anchored bottom-right. Empty-state renderer (UC-NAV-06) covers five distinct empty conditions. Two pieces of in-memory state on the screen (cleared on screen exit): `searchQuery: string` and `activeFilters: { workspaceIds: string[]; statuses: SessionStatus[] }`. `chat_sessions` and `v2_workspaces` consumed from the existing org-scoped Electric collections — **no shape changes required** (filtering happens client-side via `useLiveQuery` selectors keyed by `selectedProjectId`, `activeFilters`, and `searchQuery`, cache-first per AGENTS.md TanStack DB rule). `FlashList` tuned with explicit `estimatedItemSize` for the two-line row and stable `keyExtractor` (`session.id`) so virtualization survives filter-set changes. Row truncation order: title (1 line, ellipsis), then `branch` ellipsis before `host` before `relativeTime`. | `apps/mobile/screens/(authenticated)/(chat)/SessionsListScreen/` (NEW); subcomponents include `ProjectChip/`, `SessionSearchBar/`, `FilterButton/`, `AppliedFilterTags/`, `SessionRow/`, `NewChatFab/`, `SessionsEmptyState/` |
| **Project-picker sheet** | `@gorhom/bottom-sheet` listing accessible `v2_projects` for the active organization (UC-NAV-08 surface — renders only when the org has ≥2 projects; single-project orgs see a static project label, not a chip). Each row shows project name + workspace and session counts, derived client-side from the synced Electric collections via `useLiveQuery` (cache-first — render persisted counts even while `isReady` is false). Selection persists via the `SelectedProject` provider. | `apps/mobile/screens/(authenticated)/(chat)/ProjectPickerSheet/` (NEW) |
| **Session-filter sheet** | `@gorhom/bottom-sheet` containing two stacked multi-select sections: **Workspace** (rows = `{branch} · {hostIcon} {hostName}` from `v2_workspaces` scoped to the selected project; duplicates across hosts disambiguated; rows expose `accessibilityState={{ selected }}`) and **Status** (`⌖ Streaming`, `⚠ Pause pending`, `● Idle`). Footer with `Clear all` + `Apply` actions. No keyboard handling (no `BottomSheetTextInput`); no host-axis filter (host implied by workspace). Selections feed `activeFilters` state on `SessionsListScreen`. Sheet announces the applied count via screen-reader hint on close. | `apps/mobile/screens/(authenticated)/(chat)/SessionFilterSheet/` (NEW) |
| **New-chat workspace-picker sheet** | `@gorhom/bottom-sheet` listing workspaces belonging to the **selected project** across all hosts (`v2_workspaces` filtered by `projectId` and `organizationId`) — UC-NAV-04. Each row shows `{branch} · {hostIcon} {hostName}` so the user picks both branch and host in one tap. Selection calls `chat.createSession` and routes into the chat view (which then opens its lazy tunnel via `useChatTunnel`). | `apps/mobile/screens/(authenticated)/(chat)/NewChatSheet/` (NEW) |
| **SelectedProject provider** | Local state + `expo-secure-store` persistence of `selectedProjectId` keyed by `userId + organizationId`. First-launch default: project with most-recent activity across the synced `chat_sessions` collection, falling back to alphabetical-first. **One-time migration** on first launch post-upgrade: drops any legacy `selectedHostId` from secure-store and seeds `selectedProjectId` via the default-selection logic. Fallback path when stored `selectedProjectId` is no longer accessible (project deleted, permission revoked, org switched): silently re-run default selection and surface a brief toast. | `apps/mobile/screens/(authenticated)/(chat)/providers/SelectedProjectProvider/` (NEW) |
| **Chat-tunnel hook** (`useChatTunnel`) | Manages the **lazy relay tunnel lifecycle** for the chat session route. Opens an HTTP+tRPC tunnel against `workspace.hostId` on mount; drops it on unmount; drops on app background; re-opens on foreground while chat is mounted and triggers `useSessionResume`. **De-duplicates concurrent opens to the same `hostId`** across remounts (single in-flight per host) to avoid flapping on rapid route transitions. Surfaces `{ status: 'connecting' \| 'open' \| 'error', retry }` to chat UI for a skeleton loader during the ~300ms handshake and an inline retry banner on failure (timeout configurable, default 5s). | `apps/mobile/screens/(authenticated)/chat/[sessionId]/ChatScreen/hooks/useChatTunnel.ts` (NEW) |
| **Deep-link handler** | Routes push-notification taps to `(chat)/[sessionId]` (UC-NAV-05). On invocation: (1) await synced `v2_workspaces` collection readiness with a bounded timeout, falling back to a tRPC `chat.getSnapshot({ sessionId })` fetch if the workspace row isn't yet synced (cold-launch race); (2) resolve `workspace.projectId` from the resulting row; (3) silently update `selectedProjectId` via `SelectedProjectProvider`; (4) push the chat route — `useChatTunnel` opens the tunnel against `workspace.hostId` on mount. Invoked by the Expo notification handler. | `apps/mobile/screens/(authenticated)/(chat)/utils/handleDeepLink/` (NEW) |

> **Backend invariant.** The `chat_sessions` ElectricSQL shape remains **unchanged** in v2.0.0 (still org-scoped at `apps/electric-proxy/src/where.ts:136-137`). The project-first refactor is **entirely client-side**: filtering by `selectedProjectId` happens against the synced collection via `useLiveQuery` joins to `v2_workspaces`. No new server-side APIs, no relay protocol changes, no Electric shape extensions. Push notification payload (`{ sessionId, workspaceId, hostId, kind }`) is also unchanged — mobile derives `projectId` client-side from the synced `v2_workspaces` row, with the readiness gate above handling cold-launch races.
