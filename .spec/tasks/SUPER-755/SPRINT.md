# Sprint: SUPER-755

> Source: .spec/improvements/SUPER-755/SCOPE.md (binding, status=binding, chosen_option=moderate)
> LOC budget: 120
> Generated by: /kb-improvement-tasks-plan v1
> Bridges: kb-improvement-plan → kb-run-sprint

## Tasks

| ID | Title | Assignee | Priority | Status |
|----|-------|----------|----------|--------|
| SUPER-755-collapse-composer-model-menu | Collapse chat composer model settings into one menu | react-vite-ui-implementer | P1 | ⬜ Pending |

## Dependencies

(none — single task)

## Notes

### Assignee deviation from default map

The kb-improvement-tasks-plan default for `apps/desktop/src/renderer/**` is `electron-implementer`. This sprint deviates to **`react-vite-ui-implementer`** because:
- The work is presentation-only React component composition (no IPC, no main process, no native modules)
- The renderer is Vite-built React — squarely react-vite-ui territory
- Mirrors the investigator deviation chosen during kb-improvement-plan (`react-vite-ui-reviewer` instead of `electron-reviewer`)

### Design pre-pass skipped

The frontend-designer pre-pass was SKIPPED for this sprint. Rationale:
- `frontend-designer` already participated in the kb-improvement-plan workflow and produced an independent design pass (`improvement(SUPER-755): frontend-designer independent design pass` — commit `366715e03`)
- The full design spec (trigger appearance, menu structure, two-phase open, refinements) is embedded in `.spec/improvements/SUPER-755/SCOPE.md` under `## Proposed interface (frontend-designer)` and `## Frontend-designer review (independent design pass)`
- All 20 binding ACs already encode the design decisions verbatim (icon choices, color tokens, layout, interactions, accessibility)
- Re-running frontend-designer would produce duplicate output

The implementer should treat `SCOPE.md` itself as the design spec — its `## Proposed interface (frontend-designer)` and `## Frontend-designer review (independent design pass)` sections ARE the design constraints. The `> Design:` metadata in the task file points back to SCOPE.md instead of a separate design folder.

### Contract authority

The authoritative requirement contract is `.spec/improvements/SUPER-755/SCOPE.md`. Task files inherit ACs verbatim with the same IDs. Do NOT amend ACs in the task file — amend SCOPE.md and re-run `/kb-improvement-tasks-plan SUPER-755 --force`.

### Verification posture (per `~/.claude/CLAUDE.md` Supreme Rule)

Visual/interaction ACs (AC-1 through AC-12, AC-20) MUST be verified by running the real Electron desktop app (`bun dev` then `cd apps/desktop && bun electron:dev`, OR the project's standard dev command) and observing the composer footer in a live chat workspace. Typecheck/lint ACs (AC-18, AC-19) are command-verified. Non-regression ACs (AC-13 through AC-17) are diff-verified plus a smoke test of existing flows.
